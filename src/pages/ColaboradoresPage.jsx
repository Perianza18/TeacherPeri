import PagePlaceholder from '../components/PagePlaceholder'

// Página 9/9: Colaboradores. Contenido pendiente — asumido como una
// página sobre OTRAS personas que colaboran con TeacherPeri (distinta
// de /sobre-mi, página 8, que es sobre el creador). Confirmar cuando
// llegue el contenido real: la descripción original venía idéntica a
// la de /sobre-mi, probablemente un copy-paste sin corregir.
export default function ColaboradoresPage() {
  return <PagePlaceholder title="Colaboradores" />
}
