import PagePlaceholder from '../components/PagePlaceholder'

// Página 3/9: Experiencias — relatos de exolímpicos (vivencias, retos,
// anécdotas). Contenido pendiente.
export default function ExperienciasPage() {
  return <PagePlaceholder title="Experiencias" />
}
